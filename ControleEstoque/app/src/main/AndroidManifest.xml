<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.rafael.controleestoque">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.ListAlimentoSemanalView"></activity>
        <activity android:name=".View.CestaView" />
        <activity android:name=".View.ListVicentinoView" />
        <activity
            android:name=".View.AddVicentinoView"
            android:label="Cadastro de Vicentinos" />
        <activity
            android:name=".View.ListAlimentoView"
            android:label="Estoque" />
        <activity android:name=".View.AddAlimentoView" />
        <activity android:name=".View.MenuView" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>